import { useLocation } from "react-router-dom";

const print = () => {
  const params = useLocation()?.state;
  return (
    <div>
      {/* TODO */}
      {/* design update for print */}
    </div>
  );
};

export default print;
